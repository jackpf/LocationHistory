// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.10.1
//   protoc               v6.33.2
// source: common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "com.jackpf.locationhistory";

export enum DeviceStatus {
  DEVICE_UNKNOWN = 0,
  DEVICE_PENDING = 1,
  DEVICE_REGISTERED = 2,
  UNRECOGNIZED = -1,
}

export function deviceStatusFromJSON(object: any): DeviceStatus {
  switch (object) {
    case 0:
    case "DEVICE_UNKNOWN":
      return DeviceStatus.DEVICE_UNKNOWN;
    case 1:
    case "DEVICE_PENDING":
      return DeviceStatus.DEVICE_PENDING;
    case 2:
    case "DEVICE_REGISTERED":
      return DeviceStatus.DEVICE_REGISTERED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DeviceStatus.UNRECOGNIZED;
  }
}

export function deviceStatusToJSON(object: DeviceStatus): string {
  switch (object) {
    case DeviceStatus.DEVICE_UNKNOWN:
      return "DEVICE_UNKNOWN";
    case DeviceStatus.DEVICE_PENDING:
      return "DEVICE_PENDING";
    case DeviceStatus.DEVICE_REGISTERED:
      return "DEVICE_REGISTERED";
    case DeviceStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Device {
  id: string;
  publicKey: string;
}

export interface StoredDevice {
  device: Device | undefined;
  status: DeviceStatus;
}

export interface Location {
  lat: number;
  lon: number;
  accuracy: number;
}

function createBaseDevice(): Device {
  return { id: "", publicKey: "" };
}

export const Device: MessageFns<Device> = {
  encode(message: Device, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.publicKey !== "") {
      writer.uint32(18).string(message.publicKey);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Device {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.publicKey = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Device {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      publicKey: isSet(object.publicKey) ? globalThis.String(object.publicKey) : "",
    };
  },

  toJSON(message: Device): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.publicKey !== "") {
      obj.publicKey = message.publicKey;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Device>, I>>(base?: I): Device {
    return Device.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Device>, I>>(object: I): Device {
    const message = createBaseDevice();
    message.id = object.id ?? "";
    message.publicKey = object.publicKey ?? "";
    return message;
  },
};

function createBaseStoredDevice(): StoredDevice {
  return { device: undefined, status: 0 };
}

export const StoredDevice: MessageFns<StoredDevice> = {
  encode(message: StoredDevice, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.device !== undefined) {
      Device.encode(message.device, writer.uint32(10).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(16).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StoredDevice {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStoredDevice();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.device = Device.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StoredDevice {
    return {
      device: isSet(object.device) ? Device.fromJSON(object.device) : undefined,
      status: isSet(object.status) ? deviceStatusFromJSON(object.status) : 0,
    };
  },

  toJSON(message: StoredDevice): unknown {
    const obj: any = {};
    if (message.device !== undefined) {
      obj.device = Device.toJSON(message.device);
    }
    if (message.status !== 0) {
      obj.status = deviceStatusToJSON(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StoredDevice>, I>>(base?: I): StoredDevice {
    return StoredDevice.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StoredDevice>, I>>(object: I): StoredDevice {
    const message = createBaseStoredDevice();
    message.device = (object.device !== undefined && object.device !== null)
      ? Device.fromPartial(object.device)
      : undefined;
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseLocation(): Location {
  return { lat: 0, lon: 0, accuracy: 0 };
}

export const Location: MessageFns<Location> = {
  encode(message: Location, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lat !== 0) {
      writer.uint32(9).double(message.lat);
    }
    if (message.lon !== 0) {
      writer.uint32(17).double(message.lon);
    }
    if (message.accuracy !== 0) {
      writer.uint32(25).double(message.accuracy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Location {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLocation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 9) {
            break;
          }

          message.lat = reader.double();
          continue;
        }
        case 2: {
          if (tag !== 17) {
            break;
          }

          message.lon = reader.double();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.accuracy = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Location {
    return {
      lat: isSet(object.lat) ? globalThis.Number(object.lat) : 0,
      lon: isSet(object.lon) ? globalThis.Number(object.lon) : 0,
      accuracy: isSet(object.accuracy) ? globalThis.Number(object.accuracy) : 0,
    };
  },

  toJSON(message: Location): unknown {
    const obj: any = {};
    if (message.lat !== 0) {
      obj.lat = message.lat;
    }
    if (message.lon !== 0) {
      obj.lon = message.lon;
    }
    if (message.accuracy !== 0) {
      obj.accuracy = message.accuracy;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Location>, I>>(base?: I): Location {
    return Location.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Location>, I>>(object: I): Location {
    const message = createBaseLocation();
    message.lat = object.lat ?? 0;
    message.lon = object.lon ?? 0;
    message.accuracy = object.accuracy ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
